

server:
  port: 8767


spring:
  application:
    name: feign-hystric-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

feign:
  hystrix:
    enabled: true
